// Глобальные технические feature-флаги модуля.
// Пока задаются константой; позже источником станет настройка клуба
// (загружаемая при старте приложения) — потребители читают через
// featureFlags.* и не должны зависеть от способа получения значения.
export const featureFlags = {
  techniqueProgress: true
};
