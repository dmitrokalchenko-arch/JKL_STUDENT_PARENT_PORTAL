{
  "name": "jkl-video-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Server-side ffmpeg fallback for iPhone Safari video processing (STAGE 1 — local only, not deployed).",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test:ffmpeg": "node test/manualFfmpegTest.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "express": "^4.19.2",
    "ffmpeg-static": "^5.3.0"
  }
}
